{
  "name": "adstore-cms-proxy",
  "version": "1.0.0",
  "description": "HTTP-only proxy to Supabase tablet-cms-feed edge function for external CMS providers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  }
}
